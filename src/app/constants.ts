/* eslint-disable */
export enum AppPages {
  RootPage = '/',
  HomePage = '/home',
  Example = '/example',
  Web3 = '/web3',
  // [INSERT NEW PAGE PATH ABOVE] < Needed for generating containers seamlessly
}
